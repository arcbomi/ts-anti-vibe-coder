packages:
  # Backend microservices live under services/<service-name>.
  - "services/*"
  # Shared libraries used across services live under packages/<package-name>.
  - "packages/*"
allowBuilds:
  esbuild: set this to true or false
